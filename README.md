# C++ Telegram YouTube video downloader bot
### Dependencies instalation
```shell
sudo apt install g++ make binutils cmake libboost-system-dev libssl-dev zlib1g-dev libcurl4-openssl-dev nlohmann-json-dev
```
### Compilation
```shell
xmake
```